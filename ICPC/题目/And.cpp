#include<cstdio>
#include<iostream>
#include<vector>
#include<cstdlib>
#include<algorithm>
using namespace std;
int main()
{
	int n,x,p,a[10];
	scanf("%d%d",&n,&x);
	getchar();
	for(p=0;p<n;p++)
	{
		scanf("%d",&u);
		getchar();
		a.push_back(u);
	}
	if(n>x)
	{
		printf("0\n");
		return 0;
	}
	
}
